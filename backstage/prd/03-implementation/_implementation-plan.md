# Implementation Plan

Signal should be built in narrow, demo-safe phases. Each row below is scoped for
one small, reviewable work item.

## Phase Index

| Phase | Goal | File |
| --- | --- | --- |
| Phase 1 - Foundations | Scaffold the repo, app surfaces, documentation baseline, and typed backend pipeline | `phase-1-foundations.md` |
| Phase 2 - Live Public API Adapters | Replace fixture enrichment with cache-backed public API clients | `phase-2-live-public-api-adapters.md` |
| Phase 3 - SDR Workflow Polish | Make the product surface demo-ready and review-gated | `phase-3-sdr-workflow-polish.md` |
| Phase 4 - Rollout And Evaluation | Support the take-home narrative with evals, rollout evidence, and demo reliability | `phase-4-rollout-evaluation.md` |

## Master Thread Plan

| Status | Work Type | Goal | Story/Spec Refs | Acceptance Criteria | Likely Touch Points | Validation | Docs | Dependencies | Issue |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Done | Implementation | Establish backend lead intake, scoring, gates, drafts, and run APIs | US-1.1, US-2.1, US-2.2, US-3.1, US-3.2, US-4.1; `api-specification.md`, `data-model.md`, `agentic-framework.md`, `scoring.md`, `security.md` | Health, lead, and agent-run endpoints exist; lead creation returns enriched lead and run id; score/tier/why-line are server-generated; gate-failed leads are C-tier with no draft; run status exposes pipeline steps | `backend/app/api/v1/`, `backend/app/services/lead_service.py`, `backend/app/agents/`, `backend/app/schemas/`, `backend/tests/` | `cd backend && uv run pytest -v`; `cd backend && uv run ruff check .` | Existing PRD, API, data, agent, scoring, security docs | None | Backlog: foundations/backend |
| Done | Frontend | Establish SDR workspace scaffold for dashboard, lead queue, lead detail, agent assignment, and run detail | US-2.1, US-2.2, US-3.1, US-3.2, US-4.1; `backstage/design/README.md` | Primary routes exist; tier and score primitives render; gate-failed state is represented; static fixtures support demo navigation; layout follows desktop-first SDR workspace direction | `frontend/src/app/`, `frontend/src/components/`, `frontend/src/lib/fixtures/`, `frontend/src/types/` | `cd frontend && pnpm test`; `cd frontend && pnpm lint`; `cd frontend && pnpm typecheck` | Existing design docs and frontend scaffold notes | None | Backlog: foundations/frontend |
| Done | Implementation | Establish repo guidance and product documentation baseline | US-1.1, US-1.2, US-2.1, US-2.2, US-3.1, US-3.2, US-4.1, US-5.1; all PRD specs | AGENTS.md captures Signal-specific constraints; PRD and technical specs exist; setup and architecture docs exist; tech debt tracker captures known scaffold gaps | `AGENTS.md`, `backstage/` | Manual doc review; `rg --files backstage` | Existing docs baseline | None | Backlog: foundations/docs |
| Done | Implementation | Add cache-backed geocoding adapter and address-resolution gate | US-1.1, US-2.2; `integration-spec.md`, `data-model.md`, `agentic-framework.md`, `security.md` | Address input resolves to normalized coordinates/source facts when available; provider errors use fixture fallback; no raw provider payloads are logged | `backend/app/infrastructure/public_data/`, `backend/app/agents/nodes/`, `backend/tests/` | `cd backend && uv run pytest -v`; `cd backend && uv run ruff check .` | `integration-spec.md`, `data-model.md`, `agentic-framework.md` | Foundations/backend | Backlog: adapter/geocoding |
| Done | Implementation | Add cache-backed market demographics and economics adapters | US-2.1; `integration-spec.md`, `data-model.md`, `scoring.md`, `eval-framework.md` | Renter share, rent, household growth, and labor signals normalize into `SourceFact` records; missing fields degrade without blocking draftable leads; fixture-backed tests cover adapter success and provider merge paths; scoring inputs remain config-driven | `backend/app/infrastructure/public_data/`, `backend/app/agents/scoring.py`, `backend/tests/` | `cd backend && uv run pytest -v`; `cd backend && uv run ruff check .` | `integration-spec.md`, `scoring.md` | Geocoding adapter | Backlog: adapter/market-data |
| Done | Implementation | Add company trigger/background adapter with cited fallback facts | US-3.1; `integration-spec.md`, `agentic-framework.md`, `data-model.md`, `security.md` | Company trigger and background lookup return cited source facts when available; provider failures use cached/fixture fallback; tests cover parsed trigger/background facts | `backend/app/infrastructure/public_data/`, `backend/app/agents/nodes/`, `backend/tests/` | `cd backend && uv run pytest -v`; `cd backend && uv run ruff check .` | `integration-spec.md`, `agentic-framework.md` | Foundations/backend | Backlog: adapter/company-trigger |
| Done | Implementation | Add corporate email domain and MX validation gate | US-2.2, US-3.2; `integration-spec.md`, `scoring.md`, `security.md` | Personal domains remain hard gate failures; corporate domains validate through domain/MX checks when available; gate-failed leads never receive drafts | `backend/app/infrastructure/public_data/`, `backend/app/agents/scoring.py`, `backend/tests/` | `cd backend && uv run pytest -v`; `cd backend && uv run ruff check .` | `integration-spec.md`, `scoring.md`, `security.md` | Foundations/backend | Backlog: adapter/email-gate |
| Done | Implementation | Add demo DB seed script and reproducible A/B/C fixture reset | US-1.2, US-5.1; `api-specification.md`, `eval-framework.md`, `rollout.md` | `backend/scripts/seed_demo_leads.py` creates deterministic A/B/C, gate-failed, missing-trigger, and warning-only examples through services/repositories; seed operation requires Postgres and does not require live APIs; script reports created lead/run ids; tests prove repeatability | `backend/scripts/seed_demo_leads.py`, `backend/app/services/lead_service.py`, `backend/app/services/demo_seed.py`, `backend/tests/` | `cd backend && uv run pytest -v`; `cd backend && uv run ruff check .` | `api-specification.md`, `backstage/api/endpoints.md`, `eval-framework.md` | Foundations/backend | Backlog: demo/seed-data |
| Planned | Implementation | Wire frontend API client and typed data loading to backend responses | US-1.1, US-2.1, US-4.1; `api-specification.md`, `data-model.md`, `backstage/design/README.md` | Lead queue and agent views load through the API client; loading, error, and empty states render; TypeScript types match backend DTOs; fixtures remain available for demo fallback | `frontend/src/lib/api/client.ts`, `frontend/src/types/`, `frontend/src/app/leads/`, `frontend/src/app/agents/`, `frontend/src/lib/fixtures/` | `cd frontend && pnpm test`; `cd frontend && pnpm lint`; `cd frontend && pnpm typecheck` | Update frontend notes in `backstage/design/README.md` or setup docs if behavior changes | Backend lead and run APIs | Backlog: frontend/api-wiring |
| Planned | Frontend | Add lead queue filters, search, row navigation, and copy-draft feedback | US-2.1, US-2.2, US-3.1; `backstage/design/README.md`, `api-specification.md` | Queue supports tier/status filtering and text search; rows navigate to detail; copy draft action is review-aware and gives visible feedback; no controls appear for missing drafts | `frontend/src/app/leads/page.tsx`, `frontend/src/components/`, `frontend/src/lib/store/`, `frontend/src/lib/fixtures/` | `cd frontend && pnpm test`; `cd frontend && pnpm lint`; `cd frontend && pnpm typecheck`; desktop and narrow manual layout check | Update `backstage/design/README.md` if UI patterns change | Frontend API wiring | Backlog: frontend/queue-polish |
| Planned | Frontend | Upgrade lead detail to editable draft, source chips, related leads, and gate-failed no-draft state | US-2.2, US-3.1, US-3.2; `backstage/design/README.md`, `security.md` | Draft can be edited locally before send-like actions; every cited fact renders as a source chip; related leads/context render without blocking the page; gate-failed detail clearly shows reasons and no draft body | `frontend/src/app/leads/[id]/page.tsx`, `frontend/src/components/`, `frontend/src/types/`, `frontend/src/lib/fixtures/` | `cd frontend && pnpm test`; `cd frontend && pnpm lint`; `cd frontend && pnpm typecheck`; desktop and narrow manual layout check | Update `backstage/design/README.md`, `security.md` if safety behavior changes | Frontend API wiring | Backlog: frontend/lead-detail |
| Done | Implementation | Add agent pause and approve review-gate transitions | US-3.2, US-4.1; `api-specification.md`, `agentic-framework.md`, `security.md` | Pause and approve endpoints validate run state transitions; approval never sends outreach; run detail reflects current stage and activity log; invalid transitions return clear errors | `backend/app/api/v1/agents.py`, `backend/app/services/`, `backend/app/schemas/run.py`, `backend/tests/` | `cd backend && uv run pytest -v`; `cd backend && uv run ruff check .` | `api-specification.md`, `backstage/api/endpoints.md`, `agentic-framework.md`, `security.md` | Backend lead and run APIs | Backlog: review/agent-controls |
| Done | Implementation | Add analytics summary API for dashboard KPIs | US-1.2, US-5.1; `api-specification.md`, `rollout.md`, `eval-framework.md` | `GET /api/v1/analytics/summary` returns total leads, tier distribution, awaiting-review count, gate-failed count, average score, and top markets from repository records; tests cover seeded summary output | `backend/app/api/v1/analytics.py`, `backend/app/services/lead_service.py`, `backend/app/schemas/analytics.py`, `backend/tests/` | `cd backend && uv run pytest -v`; `cd backend && uv run ruff check .` | `api-specification.md`, `backstage/api/endpoints.md`, `eval-framework.md` | Backend lead and run APIs | Backlog: analytics/summary |
| Planned | Frontend | Add dashboard KPIs and demo queue-health visuals | US-1.2, US-5.1; `backstage/design/README.md`, `rollout.md`, `eval-framework.md` | Dashboard shows tier distribution, queue health, top markets, and demo KPIs; data works from seeded fixtures or API summary; visual states remain readable on desktop and narrow layouts | `frontend/src/app/dashboard/page.tsx`, `frontend/src/components/`, `frontend/src/lib/api/client.ts`, `frontend/src/lib/fixtures/` | `cd frontend && pnpm test`; `cd frontend && pnpm lint`; `cd frontend && pnpm typecheck`; desktop and narrow manual layout check | Update `backstage/design/README.md`, `rollout.md` if metrics change | Seed script or fixture reset | Backlog: frontend/dashboard-kpis |
| Planned | Evaluation | Build seeded demo fixture suite for stable A/B/C and gate-failed outcomes | US-1.2, US-2.1, US-2.2, US-5.1; `eval-framework.md`, `scoring.md`, `rollout.md` | Fixture suite includes A, B, C, gate-failed, missing-trigger, and warning-only cases; expected tiers and why-lines are asserted; seeded results are reproducible; drift requires explicit scoring doc update | `backend/app/infrastructure/public_data/fixtures.py`, `backend/app/agents/scoring.py`, `backend/tests/`, `backstage/prd/02-technical-docs/01-signal/` | `cd backend && uv run pytest -v`; `cd backend && uv run ruff check .` | Update `eval-framework.md`, `scoring.md`, `tech-debt-tracker.md` | Seed endpoint; adapter behavior stable enough for fixture expectations | Backlog: eval/seed-suite |
| Planned | Evaluation | Add draft citation and human-review safety evals | US-3.1, US-3.2; `eval-framework.md`, `agentic-framework.md`, `security.md` | Every personalization claim in deterministic drafts maps to a `SourceFact`; hard gate failures expose no draft; run cannot move beyond review without approval; tests cover cited and no-draft paths | `backend/app/agents/nodes/`, `backend/app/agents/prompts/`, `backend/app/infrastructure/public_data/fixtures.py`, `backend/tests/` | `cd backend && uv run pytest -v`; `cd backend && uv run ruff check .` | Update `eval-framework.md`, `security.md`, `agentic-framework.md` | Review-gate transitions; fixture suite | Backlog: eval/draft-safety |
| Planned | Implementation | Complete rollout guide, demo script, and production gap checklist | US-5.1; `rollout.md`, `security.md`, `eval-framework.md` | Rollout doc covers demo, shadow, pilot, success metrics, kill criteria, owners, and production hardening gaps; unresolved owner notes are visible; demo script references implemented screens and seeded data | `backstage/prd/02-technical-docs/01-signal/rollout.md`, `backstage/guides/`, `backstage/development/tech-debt-tracker.md` | Manual doc review; `rg -n "OWNER NOTE|TODO|Planned" backstage/prd backstage/guides backstage/development` | Update `rollout.md`, setup or guide docs as needed | Dashboard KPIs; fixture suite | Backlog: rollout/docs |

## Story Coverage Check

| Story | Covered By |
| --- | --- |
| US-1.1 | Backend lead intake; geocoding adapter; frontend API wiring |
| US-1.2 | Seed script; dashboard KPIs; seeded demo fixture suite |
| US-2.1 | Backend scoring; market adapters; queue filters; fixture suite |
| US-2.2 | Backend gates; geocoding gate; email gate; lead detail no-draft state |
| US-3.1 | Backend drafts; company trigger citations; lead detail draft/source UI; citation evals |
| US-3.2 | Backend review gate; agent pause/approve transitions; draft safety evals |
| US-4.1 | Backend agent-run APIs; frontend API wiring; agent controls |
| US-5.1 | Rollout spec; dashboard KPIs; seeded demo fixture suite; rollout guide |

## Open Owner Notes

[OWNER NOTE: Product owner input needed on pilot duration. Recommended default:
two weeks of shadow mode followed by a four-week pilot. Confirm before
implementation.]

[OWNER NOTE: Product owner input needed on production retention. Recommended
default: retain demo and pilot lead records for 90 days, then delete or
anonymize. Confirm before implementation.]
