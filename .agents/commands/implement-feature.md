# Implement Feature

Use `.agents/loops/feature-build.loop.md`.

Default prompt:

Implement the requested Signal feature as a narrow vertical slice. Read the
relevant PRD/spec docs, write focused tests first for non-trivial behavior,
implement through the correct route/service/agent/component boundary, run
verification, and update docs.
